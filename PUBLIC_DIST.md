# Public Distribution Repo

The repository root always contains the current public release bundle.
The public main branch contains one release snapshot commit.
The public repo is GHCR-first.
Large offline Docker archives are kept out of git and should be attached to public GitHub Releases when needed.